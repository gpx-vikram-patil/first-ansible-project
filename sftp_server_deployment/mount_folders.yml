# Execution
# ansible-playbook mount.yml

- name: Test mount and unmounts
  hosts: all
  become: yes
  become_user: root

  tasks:
  - name: Mount
    mount:
        path: /SFTP/File_Server_Drive/ALGORIQ/GPXSPTUSR/TEST_SFTP
        src: /SFTP/File_Server_Drive/ALGORIQ/TEST_SFTP
        opts: bind
        fstype: none
        state: mounted
